/**
 * Simulates a Refrigerator record
 */
class Junction {
  /**
         * Constructor for Restaurant object
         * @param {number} refrigeratorId foreign key
         * @param {number} inspectorId foreign key
         */
  constructor(refrigeratorId, inspectorId) {
    this.idNum = idNum;
    this.restaurantId = this.restaurantId;
    this.food = food;
  }
}

module.exports = Junction;
