/**
 * Simulates a Refrigerator record
 */
class Refrigerator {
  /**
       * Constructor for Restaurant object
       * @param {number} idNum primary key
       * @param {number} restaurantId foreign key
       * @param {string[]} food the food it contains
       */
  constructor(idNum, restaurantId, food) {
    this.idNum = idNum;
    this.restaurantId = this.restaurantId;
    this.food = food;
  }
}

module.exports = Refrigerator;
